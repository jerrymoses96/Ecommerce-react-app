const HeroContainer = () => {
  return <div className="bg-url">
    <h2>Find a host for every journey</h2>
    <p>Discover the best local rental properties that fits your every travel needs</p>
  </div>;
};

export default HeroContainer;